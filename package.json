{
  "name": "lil-gui",
  "version": "0.8.8",
  "author": {
    "name": "George Michael Brower"
  },
  "description": "Makes a floating panel for controllers on the web.",
  "module": "dist/lil-gui.module.js",
  "main": "dist/lil-gui.module.js",
  "config": {
    "style": "dist/lil-gui.css",
    "styleMin": "dist/lil-gui.min.css"
  },
  "scripts": {
    "build": "npm run icons && npm run sass && npm run sass:min && npm run rollup",
    "sass": "sass style/main.scss $npm_package_config_style --no-source-map --no-charset",
    "sass:min": "sass style/main.scss $npm_package_config_styleMin --no-charset --no-source-map --style=compressed",
    "rollup": "rollup -c",
    "lint": "eslint .",
    "icons": "node -r esm scripts/icons.js",
    "test": "node -r esm scripts/test.js",
    "api": "node -r esm scripts/api.js --write",
    "homepage": "node -r esm scripts/homepage.js",
    "server": "live-server --watch=index.html,dist,examples --no-browser",
    "preversion": "npm test",
    "version": "npm run build && git add -A dist style/icons.scss",
    "postversion": "git push --tags",
    "dev": "npm run build && node -r esm scripts/dev.js"
  },
  "files": [
    "dist"
  ],
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^4.1.1",
    "eslint": "^6.1.0",
    "eslint-plugin-html": "^6.0.0",
    "eslint-plugin-jsdoc": "^15.9.1",
    "esm": "^3.2.25",
    "handlebars": "^4.3.1",
    "highlight.js": "^9.15.10",
    "jsdoc-api": "^5.0.2",
    "live-server": "^1.2.1",
    "markdown-it": "^9.1.0",
    "onchange": "^6.0.0",
    "rollup": "^1.17.0",
    "rollup-plugin-terser": "^5.1.2",
    "sass": "^1.22.10",
    "webfont": "^9.0.0"
  },
  "eslintIgnore": [
    "dist",
    "examples/vendor"
  ]
}
